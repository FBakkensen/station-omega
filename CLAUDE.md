# CLAUDE.md

Repository guidance is maintained in `AGENTS.md`.

Use `AGENTS.md` as the authoritative source for commands, architecture, testing, and project conventions. This file stays intentionally minimal to avoid duplicated instructions drifting out of sync.

## Rules

- All failing linters and tests must be treated as your responsibility. Never ignore or dismiss failures because they were not introduced in the current session. If `bun run check` fails, investigate and fix every issue before completing work.